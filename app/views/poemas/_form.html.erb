<%= form_with(model: poema, local: true) do |form| %>
  <% if poema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poema.errors.count, "error") %> prohibited this poema from being saved:</h2>

      <ul>
        <% poema.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_area :titulo %>
  </div>

  <div class="field">
    <%= form.label :contenido %>
    <%= form.text_area :contenido %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
